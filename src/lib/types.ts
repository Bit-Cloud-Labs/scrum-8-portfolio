export interface Project {
  title: string;
  description: string;
  tech_stack: string[];
  screenshot?: string;
  github_url?: string;
  live_demo_url?: string;
}

export interface Skill {
  name: string;
}
